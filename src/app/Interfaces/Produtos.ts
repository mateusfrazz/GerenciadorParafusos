export interface Produtos {
    id?: number;  // Mudei para string pois seu JSON mostra "4" (entre aspas)
    produto: string;
    valor: number; // Mudei para string pois seu JSON mostra "1200" (entre aspas)
}